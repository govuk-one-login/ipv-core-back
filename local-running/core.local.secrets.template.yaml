# Local running secrets - copy this file to `core.local.secrets.yaml` and update secrets accordingly
---
core:
  self:
    ci-config: 'PLACEHOLDER'
    evcs:
      api-key: '{"apiKey":"PLACEHOLDER"}'
    gov-uk-notify:
      api-key: "PLACEHOLDER"
  credential-issuers:
    dcmawAsync:
      connections:
        stub:
          oauth-client-secret: "PLACEHOLDER" # pragma: allowlist secret
