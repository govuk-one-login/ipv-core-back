name: MFA Reset

description: >-
  The routes a user can take to validate their
  account so that they can reset their two
  factor authentication mechanism.

states:
  # Entry points

  START:
    events:
      next:
        # should be filled out in https://govukverify.atlassian.net/browse/PYIC-6486
        targetState: PLACEHOLDER

  # Journey states

  PLACEHOLDER:
    response:
      type: page
      pageId: pyi-technical
      context: unrecoverable
